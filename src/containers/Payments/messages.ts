/*
 * FloatingButtons component messages
 */

export const scope = 'app.containers.payments';

const messages = {
  selectPayment: {
    scope: `${scope}.selectPayment`,
    options: {
      defaultValue: 'Select your payment method',
    },
  },
};

export default messages;
