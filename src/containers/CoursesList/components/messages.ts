/*
 * CourseList Container  messages
 */

export const scope = 'app.screen.courseList.components.course';

const messages = {
  sessionNo: {
    scope: `${scope}.sessionNo`,
    options: {
      defaultValue: 'Session no.',
    },
  },
};

export default messages;
