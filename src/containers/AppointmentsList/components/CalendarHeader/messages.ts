/*
 * CalendarHeader component messages
 */

export const scope =
  'app.conatainers.appointmentList.components.calendarHeader';

const messages = {
  day: {
    scope: `${scope}.day`,
    options: {
      defaultValue: 'Day',
    },
  },
  _3days: {
    scope: `${scope}._3days`,
    options: {
      defaultValue: '3 Days',
    },
  },
  week: {
    scope: `${scope}.week`,
    options: {
      defaultValue: 'Week',
    },
  },
};

export default messages;
