/*
 * ContactsList Component messages
 */

export const scope = 'app.containers.contactsList';

const messages = {
  client: {
    scope: `${scope}.client`,
    options: {
      defaultValue: 'Client',
    },
  },
  selectClient: {
    scope: `${scope}.selectClient`,
    options: {
      defaultValue: 'Select Client',
    },
  },
};

export default messages;
