import React from 'react';
import { Text, View, TextInput, Button, Alert, StyleSheet } from 'react-native';

const AddContactScreen: React.FC<IAddContactScreenProps> = (props) => {
  return (
    <>
      <Text style={{ color: 'black' }}>AddContactScreen</Text>
    </>
  );
};
export interface IAddContactScreenProps {}
export default AddContactScreen;
